// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import { env, isSupabaseConfigured } from '@/lib/env-safe';
import type { Database } from './types';

// Supabase 클라이언트 안전한 초기화
function createSupabaseClient() {
    try {
        if (!isSupabaseConfigured()) {
            console.warn('🚧 Supabase: Using placeholder configuration');
            console.warn('📋 Database features disabled');
            console.warn('   To enable: Add environment variables in Vercel Dashboard');
            console.warn('   VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY');

            // 안전한 더미 클라이언트
            return createClient(
                'https://placeholder.supabase.co',
                'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBsYWNlaG9sZGVyIiwicm9sZSI6ImFub24iLCJpYXQiOjE2NDk3NzEyMDAsImV4cCI6MTk2NTM0NzIwMH0.fake',
                {
                    auth: {
                        persistSession: false,
                        autoRefreshToken: false,
                        detectSessionInUrl: false
                    },
                    realtime: { enabled: false }
                }
            );
        }

        console.log('✅ Supabase: Connected successfully');
        return createClient<Database>(
            env.SUPABASE_URL,
            env.SUPABASE_ANON_KEY,
            {
                auth: {
                    persistSession: true,
                    storageKey: 'rin-korea-auth',
                    autoRefreshToken: true,
                    detectSessionInUrl: true
                },
                realtime: {
                    enabled: true,
                    params: {
                        eventsPerSecond: 2
                    }
                },
                global: {
                    headers: {
                        'x-my-custom-header': 'rin-korea-app'
                    }
                }
            }
        );
    } catch (error) {
        console.error('❌ Supabase: Failed to create client:', error);

        // 폴백 클라이언트
        return createClient(
            'https://placeholder.supabase.co',
            'placeholder-key',
            {
                auth: {
                    persistSession: false,
                    autoRefreshToken: false,
                    detectSessionInUrl: false
                },
                realtime: { enabled: false }
            }
        );
    }
}

export const supabase = createSupabaseClient();
export { isSupabaseConfigured };

// 개발 환경에서 상태 표시
if (env.DEV) {
    console.log('🔧 Supabase Configuration Status:', {
        configured: isSupabaseConfigured(),
        url: isSupabaseConfigured() ? '✅ Configured' : '❌ Missing VITE_SUPABASE_URL',
        key: isSupabaseConfigured() ? '✅ Configured' : '❌ Missing VITE_SUPABASE_ANON_KEY'
    });
}
